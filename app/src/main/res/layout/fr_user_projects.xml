<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:bind="http://schemas.android.com/apk/res-auto">

    <data class="UserProjectsBinding">

        <import
            alias="V"
            type="android.view.View" />

        <variable
            name="uvm"
            type="com.elegion.test.behancer.ui.userprojects.UserProjectsViewModel" />

    </data>

    <android.support.v4.widget.SwipeRefreshLayout
        android:id="@+id/refresher"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        bind:onRefresh="@{uvm.onRefreshListener}"
        bind:refreshState="@{uvm.isLoading}">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical">

            <android.support.v7.widget.RecyclerView
                android:id="@+id/recycler"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:visibility="@{uvm.isErrorVisible ? V.GONE : V.VISIBLE}"
                />

            <include
                layout="@layout/v_error"
                android:visibility="@{uvm.isErrorVisible ? V.VISIBLE : V.GONE}" />

        </LinearLayout>

    </android.support.v4.widget.SwipeRefreshLayout>

</layout>